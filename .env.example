PORT=5000
JWT_SECRET=change_this_to_a_long_random_string
# Optional. If set, the AI planner calls the real Claude API to generate
# each itinerary. If left blank, a built-in constraint-based planner runs
# instead, so the app works fully with zero configuration.
ANTHROPIC_API_KEY=
